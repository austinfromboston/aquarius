.ui-block
  %h6.info New Person
  - form_for @person, :url => people_url, :html => { :class => 'badge' } do |f|
    = render :partial => 'form', :locals => { :f => f }
    %p.submit
      = f.submit "Create"

